"use client";

import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Icon } from "@iconify/react";

const TransactionApprovalForm = () => {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Transaction Approval Form</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Approval form implementation coming soon...</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default TransactionApprovalForm;

"use client";

import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Icon } from "@iconify/react";

const TransactionCompletionForm = () => {
  return (
    <div className="space-y-6">
      <Card>
        <CardHeader>
          <CardTitle>Transaction Completion Form</CardTitle>
        </CardHeader>
        <CardContent>
          <p>Completion form implementation coming soon...</p>
        </CardContent>
      </Card>
    </div>
  );
};

export default TransactionCompletionForm;
